# Core dependencies for Linux Driver Evaluation Framework

# Configuration and data handling
pydantic>=1.10.0
pyyaml>=6.0
click>=8.0.0

# Static analysis tools (will be installed separately)
# clang-tools (system package)
# sparse (system package) 
# coccinelle (system package)

# Web framework for API
fastapi>=0.95.0
uvicorn>=0.20.0
python-multipart>=0.0.6

# Report generation
jinja2>=3.1.0
weasyprint>=58.0  # for PDF generation

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Development tools
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0